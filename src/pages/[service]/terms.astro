---
import { getServiceIds, defaultLang, type ServiceId } from '../../config';

export function getStaticPaths() {
  return getServiceIds().map((service) => ({ params: { service } }));
}

const { service } = Astro.params as { service: ServiceId };

return Astro.redirect(`/${service}/${defaultLang}/terms`);
---
